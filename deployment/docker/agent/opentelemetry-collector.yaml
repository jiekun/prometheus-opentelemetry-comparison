receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:

exporters:
  otlp_http/prometheus:
    endpoint: "http://10.140.15.201:9090/api/v1/otlp"
    compression: snappy
    timeout: 10s
    tls:
      insecure: true

service:
  pipelines:
    metrics:
      receivers: [ otlp ]
      processors: [ batch ]
      exporters: [ otlp_http/prometheus ]


receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    send_batch_size: 10000
    timeout: 1s

extensions:
  file_storage:
    directory: /home/jiekun/prometheus-opentelemetry-comparison/bin/otel-agent-storage

exporters:
  # Prometheus running as a full backend on the storage host
  # (10.140.15.205), listening on its default port 9090, ingesting via its
  # native OTLP receiver (--web.enable-otlp-receiver, see
  # deployment/docker/backend/Makefile's run-prometheus-server).
  otlp_http/prometheus:
    sending_queue:
      storage: file_storage
    metrics_endpoint: "http://10.140.15.205:9090/api/v1/otlp/v1/metrics"
    compression: gzip
    timeout: 10s
    tls:
      insecure: true

service:
  extensions: [file_storage]
  pipelines:
    metrics:
      receivers: [ otlp ]
      processors: [ batch ]
      exporters: [ otlp_http/prometheus ]

  # Self-observability: exposes the collector's own internal metrics as a
  # Prometheus-scrapeable endpoint (see prometheus-server's prometheus.yaml
  # for the matching scrape job). In particular this includes
  # otelcol_receiver_accepted_metric_points - the sample count this agent
  # has actually received from otel-app - alongside its own resource usage
  # (otelcol_process_*). without_type_suffix defaults to true for this
  # exporter, so the accepted-points counter is exposed without a
  # trailing _total.
  telemetry:
    metrics:
      level: basic
      readers:
        - pull:
            exporter:
              prometheus:
                host: "0.0.0.0"
                port: 8888

